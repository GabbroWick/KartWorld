# Godot 4 generated files
.godot/
/android/
/builds/
*.translation

# Export artefacts. export_presets.cfg is tracked: the Web preset holds no
# secrets, and a reproducible export is worth more than hiding it.

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# Raw asset downloads (zips); curated copies live in assets/models/
art/
screenshot.png
